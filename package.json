{
  "name": "new",
  "version": "1.0.0",
  "description": "",
  "main": "advisor.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "app:build": "node copy-assets.js",
    "app:init": "npm run app:build && cap init \"Tax Calculator\" \"com.kthur.taxcalculator\" --web-dir=www",
    "app:add-android": "npm run app:build && cap add android",
    "app:add-ios": "npm run app:build && cap add ios",
    "app:sync": "npm run app:build && cap sync",
    "app:open-android": "cap open android",
    "app:open-ios": "cap open ios"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "playwright": "^1.61.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@playwright/test": "^1.61.1"
  }
}
